{
  "name": "kakotsu-main",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "deploy": "git subtree push --prefix build origin gh-pages && git add . && git commit -m 'deploy` && git push && git checkout gh-pages && git add . && git commit -a 'deploy' && git push && git checkout main" 
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^1.0.0-next.21",
    "@sveltejs/kit": "next",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-svelte3": "^3.2.1",
    "gh-pages": "^3.2.3",
    "prettier": "^2.4.1",
    "prettier-plugin-svelte": "^2.4.0",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "@fontsource/fira-mono": "^4.5.0",
    "@lukeed/uuid": "^2.0.0",
    "cookie": "^0.4.1",
    "svelte-preprocess": "^4.9.8",
    "sveltestrap": "^5.6.3",
    "typescript": "^4.5.2"
  }
}
